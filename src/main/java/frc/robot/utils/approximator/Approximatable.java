package frc.robot.utils.approximator;

/**
 * An approximatable thing implements a function that approximates its input
 */
public interface Approximatable {
    public double approximate(double x);
}
